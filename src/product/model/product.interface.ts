export enum ProductStatus {
    ALL = 'ALL',
    TEST = 'TEST',
    ENTERED = 'ENTERED',
    DISABLED = 'DISABLED',
}

export enum HashtagType {
    PRODUCT = 'PRODUCT',
    USER = 'USER'
}

export enum EventType {
    ALL = 'ALL',
    PROMOTION = 'PROMOTION',
    SURVEY = 'SURVEY',
}

export enum EventStatus {
    ALL = 'ALL',
    CREATED = 'CREATED',
    RUNNING = 'RUNNING',
    END = 'END',
}